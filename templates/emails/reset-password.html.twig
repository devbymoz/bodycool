{% extends "base-email.html.twig" %}

{% block titre %}Modifier votre mot de passe{% endblock %}

{% block content %}
    <p>Bonjour, vous avez demandé à changer de mot de passe, voici le lien pour vous permettre de le modifier</p>

    <a href="{{ absolute_url(path('app_creer.mot.de.passe', {'token' : user.getActivationToken})) }}" ><button type="submit">Réinitialiser mon mot de passe</button></a>
    <br>
    <p>Si le bouton ne fonctionne pas, vous pouvez cliquer sur le lien suivant : </p> 

    <a href="{{ absolute_url(path('app_creer.mot.de.passe', {'token' : user.getActivationToken})) }}">{{ absolute_url(path('app_creer.mot.de.passe', {'token' : user.getActivationToken})) }}</a>

    <p>Si vous n'êtes pas à l'origine de cette demande, merci d'ignorer cet email.</p> 
{% endblock %}

